# Docker Environment Configuration
# This file contains environment variables for running the application in Docker
# Copy this file to .env.docker or .env and update the values as needed

# Application Configuration
PORT=3000
APP_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3000
NODE_ENV=production

# Database Configuration
# Use 'db' as hostname (Docker service name) when running in Docker Compose
DATABASE_URL=postgresql://flowgenie_user:flowgenie_password@db:5432/flowgenie_db

# Database connection components (used by docker-compose.yml)
DB_USER=flowgenie_user
DB_PASSWORD=flowgenie_password
DB_NAME=flowgenie_db
DB_PORT=5432

# Redis Configuration
# Use 'redis' as hostname (Docker service name) when running in Docker Compose
REDIS_URL=redis://redis:6379
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=604800

# Google OAuth2 Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

